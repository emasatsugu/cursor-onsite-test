# Demo workspace for the VM file-editing agent POC.

This tree is what the agent tools operate on. Paths in tool calls are
relative to this workspace root.

Try prompts like:
- "Read src/hello.js and update the greeting"
- "Add a one-line note to this README"
